# 99 — Human Manual Action Items
# Things only CS can do (IDE setup, external accounts, app stores, etc.)
# Any agent session can append items here.
# PM reviews and organizes by priority each cycle.

---

## PENDING — Work through by priority

### P0 — Blocks v0.1.0 Release
- [ ] **Integrate core modules into auto-agent.sh** — Source all 8 modules (`src/core/*.sh`). Full step-by-step in `src/core/INTEGRATION-GUIDE.md`. This is the #1 blocker for v0.1.0.
- [ ] **Fix eval injection (HIGH-01)** — Replace `eval` in `commit_by_ownership()` (auto-agent.sh:236-241) with array-based args. Exact replacement code in `src/core/INTEGRATION-GUIDE.md`.
- [ ] **Fix PowerShell notify (MEDIUM-02)** — Escape variable in notify function. Replacement code in `src/core/INTEGRATION-GUIDE.md`.
- [ ] **Add `set -e` to auto-agent.sh** — CTO requests at line 23. Prevents silent failures.
- [ ] **Reconcile agents.conf (BUG-009)** — Two divergent copies: root has 13 agents, scripts/ has 8. Pick one source of truth.

### P1 — Before v0.1.0 Tag
- [ ] **Update CLAUDE.md** — QA found it lists 11 agents and paths (src-tauri/, ios/) that don't exist yet. Align with reality.
- [x] **Fix QA prompt path reference** — FIXED by PM (cycle 2): `prompts/07-qa/` → `prompts/09-qa/`
- [x] **Fix Security prompt path reference** — FIXED by PM (cycle 2): `prompts/08-security/` → `prompts/10-security/`
- [ ] **Logo/branding assets** — Web agent needs logo in `assets/branding/` (currently text-only)
- [x] **Update .gitignore (MEDIUM-01)** — FIXED by backend (cycle 2): `.env`, `*.pem`, `*.key`, `credentials.json` added
- [ ] **README rewrite** — CEO says this must happen before v0.1.0 tag

### P2 — After v0.1.0
- [ ] **Deploy landing page** — Set up Vercel or GitHub Pages for `site/`
- [ ] **Remove orphaned `prompts/01-pm/` directory (BUG-008)** — Old directory from before PM moved to 03-pm

---

## DONE

(Move completed items here with date)
